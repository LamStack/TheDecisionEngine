# Everything in this file is OPTIONAL. The app runs fully without any of it —
# see README "Running with zero configuration".

# When set, free-text fields (refund reasons, ticket messages, commit
# summaries, flagged content) are analyzed by Claude Haiku instead of the
# built-in regex/lexicon heuristic. Same output shape either way; the
# decision logic does not change based on which one ran.
# Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=
